server:
  port: 8081
spring:
  data:
    redis:
      host: ${SPRING_REDIS_HOST}
      port: 6379
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
  datasource:
    url: jdbc:h2:mem:database

mqqt-config:
  server-uri: tcp://mosquitto:1883